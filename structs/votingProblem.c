# include <stdio.h>

int main() {
    int votes[7] = {0};
    int n, invalid = 0;
    while (n != 0) {
        scanf("%d ", &n);
        if (n < 7 && n > 0) {
            votes[n]++;
        } 
        else {

        }
    }
}